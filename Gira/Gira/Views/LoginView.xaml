<Window x:Class="Gira.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gira"
        mc:Ignorable="d"
        Title="Login / Register" Height="475" Width="350"
        ResizeMode="NoResize"
        Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}"
        Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Frame x:Name="frMain" Grid.Row="0" NavigationUIVisibility="Hidden"/>

        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 0 0 15" Grid.Row="1">                                
            <Hyperlink RequestNavigate="Hyperlink_RequestNavigate" NavigateUri=" " Style="{StaticResource DefaultHyperlinkStyle}">
                <TextBlock Name="tbkChangePage" Text="Placeholder" FontSize="14"/>
            </Hyperlink>
        </TextBlock>
    </Grid>
</Window>
