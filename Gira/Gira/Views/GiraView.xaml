<Window x:Class="Gira.GiraView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gira"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}"
        Foreground="{DynamicResource SystemControlPageTextBaseHighBrush}"
        Title="Gira" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gira" Margin="15" Style="{StaticResource HeaderTextBlockStyle}" />

        <TextBlock HorizontalAlignment="Right" Margin="0 15 15 15"  Grid.Row="0">                                
            <Hyperlink RequestNavigate="Hyperlink_RequestNavigate" NavigateUri=" " Style="{StaticResource DefaultHyperlinkStyle}">
                <TextBlock Text="Logout" FontSize="14"/>
            </Hyperlink>
        </TextBlock>

        <Frame x:Name="frMain" Grid.Row="1"  NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
    
